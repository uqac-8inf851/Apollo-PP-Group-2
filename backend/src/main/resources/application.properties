## default connection pool
#spring.datasource.hikari.connectionTimeout=20000
#spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/apollo-dev
spring.datasource.username=apollo-user
spring.datasource.password=@pollo

#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=create

spring.application.name=apollo
logging.file.name=/var/apollo/apollo.log